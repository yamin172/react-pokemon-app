import React from 'react';
import Search from '../Search/Search';

const Home = () => {
    return (
        <div className="container mx-auto">
            <Search />
        </div>
    );
};

export default Home;